version: 2

build:
   os: ubuntu-22.04
   tools:
      python: "3.9"

python:
   install:
      - requirements: docs/requirements.txt
      - method: pip
        path: .

spinx:
   configuration: docs/source/conf.py

formats: []